flowchart TD
A[识别微分方程] --> B{判断方程阶数};
    
B -->|一阶方程| C{判断一阶方程类型};
C -->|可分离变量| D[分离变量后积分];
C -->|齐次方程| E[令u=y/x代换];
C -->|线性方程<br>y'+P(x)y=Q(x)| F[使用一阶线性通解公式];
C -->|伯努利方程| G[令z=y^(1-n)化为线性];
C -->|全微分方程| H[验证∂P/∂y=∂Q/∂x<br>求原函数u(x,y)];
    
B -->|高阶方程| I{是否可降阶?};
I -->|y^(n)=f(x)| J[连续积分n次];
I -->|不显含y<br>y''=f(x,y')| K[令p=y' 降阶];
I -->|不显含x<br>y''=f(y,y')| L[令p=y', y''=p dp/dy 降阶];
I -->|否，为高阶线性| M{是否为常系数?};
    
M -->|是，二阶常系数<br>y''+py'+qy=f(x)| N;
subgraph N[二阶常系数求解步骤]
    N1[写出特征方程<br>r²+pr+q=0] --> N2[求齐次通解Y];
    N2 --> N3[根据f(x)形式设特解y*];
    N3 --> N4[利用待定系数法<br>确定y*];
end
N --> O[得非齐次通解 y=Y+y*];
    
M -->|变系数或高阶| P[考虑常数变易法、<br>欧拉方程代换等特殊方法];